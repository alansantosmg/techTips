# Git Commands

```git
git add 
git rm 
git rm --cached  
git mv 
git status 
git diff 
git commit -m 
git commit -a -m 'msg' 
.git/.gitignore 
git rm log/\*.log 
git rm \*~ 
git clone git:// 
git remote add meudiretorio git:// 
git fetch meudiretorio 
git push meudiretorio master 
git push origin master 
 
git log 
git log -p -2 
git log --stat 
git log --pretty=oneline 
git log --pretty=full 
git log --pretyy=short 
git log --prety=fuller 
git log --pretty=format:"%h - %an, %ar : %s" 
git log --pretty=format"%h %s" --graph 

git commit --amend 

git tag -a nomedatag -m 'minha mensagem' 
git show (tag) 
git push origin (nomedatag) 
git push orign --tags 
```
